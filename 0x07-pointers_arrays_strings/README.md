* **POINTERS, ARRAYS AND STRINGS**
* **POINTER**
A pointer is the address of a memory location (which may or may not hold a value)
* **ARRAY**
An array is a data type that holds sequences of data in a contiguous memory location
* **STRINGS**
Strings in C are sequences of characters that are null-terminated

